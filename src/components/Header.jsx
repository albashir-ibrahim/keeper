import React from "react"
import CreateIcon from '@material-ui/icons/Create';

function Header() {
    return <header>
        <h1>Bash Notes<CreateIcon /></h1>
    </header>
}

export default Header;